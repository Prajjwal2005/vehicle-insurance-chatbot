# ── Insurance System API: runtime dependencies ──
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0,<2.1
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1        # enables Pydantic EmailStr validation

# Database drivers
aiosqlite>=0.20        # async SQLite — local development (zero setup)
asyncpg>=0.29          # async PostgreSQL — container / production
